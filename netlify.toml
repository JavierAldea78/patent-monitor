[build]
  command   = "mkdir -p _site && cp index.html patents.csv patents_readable.txt _headers _site/ && cp patents.json _site/patents.json"
  publish   = "_site"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-store"
